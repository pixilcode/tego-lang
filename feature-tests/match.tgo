main = matchBool true, matchInt 1, matchChar 'a', matchStr "a"

matchBool b =
	match b to
	| true -> true
	| _ -> false

matchInt i =
	match i to
	| 1 -> true
	| _ -> false

matchChar c =
	match c to
	| 'a' -> true
	| _ -> false

matchStr s =
	match s to
	| "a" -> true
	| _ -> false
